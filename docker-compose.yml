version: '3.8'

services:
  freebie:
    build: .
    container_name: freebie
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - ./config.json:/app/config.json:ro
    environment:
      - FREEBIE_CONFIG=/app/config.json
